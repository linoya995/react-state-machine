{
  "compilerOptions": {
    "target": "ESNext", // Target the latest ECMAScript version
    "module": "ESNext", // Use ECMAScript modules
    "declaration": true, // Generate declaration files
    "strict": true // Enable strict type checking
    // Additional compiler options as needed
  },
  "include": ["index.ts"], // Include TypeScript files within the src directory
  "exclude": ["node_modules"] // Exclude node_modules from TypeScript processing
}
